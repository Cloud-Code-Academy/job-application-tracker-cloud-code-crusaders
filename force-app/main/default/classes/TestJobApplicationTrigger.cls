@istest
public with sharing class TestJobApplicationTrigger {
    
   // @istest
    //public static void testJobApplicationTrigger_updateTask(){
      //  Job_Application__c newJob = new Job_Application__c(Name = 'Banana Co', Status__c = 'Saved');
        //insert newJob;
    //}
    
  //  System.assertEquals(tasks.size(), 1, 'Task did not update')

}